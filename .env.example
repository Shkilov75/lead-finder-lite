# Copy to .env.local to override. Nothing here is required — the defaults are
# what `npm run dev` and `npm run dev:api` use.

# Where next.config.ts forwards /api/* requests. Server-side only; the browser
# never sees this value.
BACKEND_ORIGIN=http://localhost:8000
